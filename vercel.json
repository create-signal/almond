{
  "version": 2,
  "builds": [
    {
      "src": "./dist/api/index.js",
      "use": "@vercel/node"
    }
  ],
  "buildCommand": "npm run build",
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }]
}
